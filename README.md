User with SessionStorage (interface, abstract class, legacy, ...)
=================================================================

- The User is loaded from the session with __construct()

- The User is stored in the session with __destruct()